# Optional. In local dev, leave unset so the app uses same-origin /api (Vite proxy).
# Setting http://localhost:8000/api breaks API calls when you open the app at http://127.0.0.1:5173.
# VITE_API_URL=http://localhost:8000/api
